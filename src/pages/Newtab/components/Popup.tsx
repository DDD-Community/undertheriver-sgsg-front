import React from "react";
/** @jsx jsx */
import { css, jsx } from '@emotion/react';
const popupWrapper = css`
  display: none;
`;
const Popup = (props:any) => {
    return (
     // <main css={popupWrapper}>
     <main>
        test!
     </main>
    )
}
export default Popup;